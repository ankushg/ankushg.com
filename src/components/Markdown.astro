---
import { renderMarkdownToHtml } from '../utils/renderMarkdown'
interface Props {
  markdown: string
}
const { markdown } = Astro.props

const renderedPayload = renderMarkdownToHtml(markdown)
---

<Fragment set:html={renderedPayload} />
